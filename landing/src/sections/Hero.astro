---
import { Button } from '@nexora/ui';
import { Icon } from 'astro-icon/components';
---

<div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,hsl(252_87%_67%/0.12),transparent_60%)]"></div>
<section class="relative container mx-auto flex h-dvh max-w-4xl flex-col items-center justify-center px-6 text-center">
  <span class="border-border text-muted-foreground mb-6 inline-flex items-center gap-2 rounded-full border px-4 py-1.5 text-sm">
    <Icon name="lucide:zap" class="text-primary size-3.5" />
    Ahora con análisis en tiempo real
  </span>

  <h1 class="mb-6 text-5xl font-bold tracking-tight md:text-7xl">
    Transformar eventos físicos en <span class="text-gradient">Experiencias Interactivas</span>
  </h1>

  <p class="text-muted-foreground mx-auto mb-10 max-w-2xl text-lg md:text-xl">
    Los visitantes obtienen insignias, completan recorridos y se mantienen activos. La plataforma integral para organizadores de eventos que
    buscan un impacto medible.
  </p>

  <div class="flex flex-col items-center justify-center gap-4 sm:flex-row">
    <Button size="lg" class="w-full sm:w-fit" disabled>Comienza tu evento</Button>
    <Button variant="outline" size="lg" class="w-full sm:w-fit" disabled>Ver demostración</Button>
  </div>
</section>
